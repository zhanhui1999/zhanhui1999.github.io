name: test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: build
      run: |
          pwd
          ls
          rm -rf ./hadoop-hive-image
          git clone https://github.com/zhanhui1999/hadoop-hive-image.git
          cd ./hadoop-hive-image
          git checkout main
